# scale-comparison

This app is meant as a tool to render opinionated animations that visually compare different `things` with values ranging from really small to extremely big. The current unit of things being compared is seconds. This project is very experimental.
